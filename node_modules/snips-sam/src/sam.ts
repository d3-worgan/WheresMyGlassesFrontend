import * as yargs from 'yargs'

export async function run() {
    // tslint:disable-next-line:no-unused-expression
    yargs
        .commandDir('commands')
        .demandCommand(1, 10)
        .strict()
        .help()
        .argv
}
