import * as yargs from 'yargs'

export const command = 'setup'
export const desc = 'Setup hardware'
export const builder = (argv: yargs.Argv) => argv.commandDir('setup')
export const handler = (_: yargs.Argv) => {}
