export const newError = (message: string = '', name: string = ''): Error => {
    const error = new Error(message)
    error.name = name
    return error
}

export const NO_CREDENTIALS_FOUND = 'NO_CREDENTIALS_FOUND'
export const SSH_CONNECT_ERROR = 'SSH_CONNECT_ERROR'
